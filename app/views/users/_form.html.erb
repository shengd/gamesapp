<div class="row">
  <div class="span6 offset3">
    <%= form_for(@user) do |f| %>
      <%= render 'shared/error_messages' %>

      <%= render "form_#{yield(:usr_form_type)}", form: f %>
    <% end %>
  </div>
</div>
